import Logo from '../ui/logo';
import Nav from '../nav/nav';

const MainHeader = () => {
  return (
    <header className='container'>
      <Logo />
      <Nav />
    </header>
  );
};

export default MainHeader;
